services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9091:9090"
    volumes:
      - ./config:/etc/prometheus:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
